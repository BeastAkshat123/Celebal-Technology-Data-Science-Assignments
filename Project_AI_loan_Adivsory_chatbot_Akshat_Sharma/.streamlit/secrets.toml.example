# FOR DEPLOYMENT ONLY (Streamlit Community Cloud).
# Do not fill this in locally -- use .env instead for local runs.
#
# On Streamlit Cloud: go to your app -> Settings -> Secrets, and paste the
# contents of this file (with your real key) into that box directly.
# You do NOT need to commit a real secrets.toml to GitHub -- and you
# shouldn't, since .gitignore already excludes it.

GROQ_API_KEY = "gsk_your_key_here"
